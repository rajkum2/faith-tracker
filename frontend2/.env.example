# Data source: "json" for local JSON files, "api" for real API backend
NEXT_PUBLIC_DATA_SOURCE=json

# API base URL (only used when NEXT_PUBLIC_DATA_SOURCE=api)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api
